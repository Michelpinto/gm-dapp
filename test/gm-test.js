const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Gm", function () {
    it("allows user to send eth to another address", async function () {
        // To be done soon
    });
});
